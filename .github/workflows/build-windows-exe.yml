name: Build Windows Portable EXE

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build-windows-exe:
    name: Build Windows EXE
    runs-on: windows-2022
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.11 x64
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          architecture: 'x64'

      - name: Install dependencies
        shell: powershell
        run: |
          python -m pip install --upgrade pip setuptools wheel
          python -m pip install pyinstaller numpy matplotlib openpyxl pillow pandas scipy

      - name: Verify source and imports
        shell: powershell
        run: |
          python --version
          python - <<'PY'
          import sys
          print(sys.executable)
          import tkinter
          print("tkinter OK", tkinter.TkVersion)
          import matplotlib
          print("matplotlib OK", matplotlib.__version__)
          import numpy
          print("numpy OK", numpy.__version__)
          try:
              import scipy
              print("scipy OK", scipy.__version__)
          except Exception as e:
              print("scipy import failed:", e)
          import openpyxl
          print("openpyxl OK", openpyxl.__version__)
          PY
          python -m py_compile capillimetry_windows.py

      - name: Build portable folder
        shell: powershell
        run: |
          $ErrorActionPreference = "Stop"

          pyinstaller `
            --noconfirm `
            --clean `
            --onedir `
            --windowed `
            --name Capillimetry `
            --hidden-import=tkinter `
            --hidden-import=tkinter.ttk `
            --hidden-import=tkinter.filedialog `
            --hidden-import=tkinter.messagebox `
            --hidden-import=matplotlib `
            --hidden-import=matplotlib.backends.backend_tkagg `
            --hidden-import=numpy `
            --hidden-import=scipy `
            --hidden-import=scipy.interpolate `
            --hidden-import=openpyxl `
            --hidden-import=pandas `
            --collect-all matplotlib `
            --collect-all numpy `
            --collect-all scipy `
            --collect-all openpyxl `
            --collect-all pandas `
            capillimetry_windows.py

          if (!(Test-Path "dist\Capillimetry\Capillimetry.exe")) {
            throw "EXE was not created"
          }

          Copy-Item "PORTABLE_WINDOWS_README.txt" "dist\Capillimetry\README.txt" -Force

      - name: Zip portable folder
        shell: powershell
        run: |
          if (Test-Path "CapillimetryPortable.zip") {
            Remove-Item "CapillimetryPortable.zip" -Force
          }
          Compress-Archive -Path "dist\Capillimetry\*" -DestinationPath "CapillimetryPortable.zip" -Force
          Get-ChildItem "dist\Capillimetry" | Select-Object Name,Length
          Get-Item "CapillimetryPortable.zip"

      - name: Upload portable ZIP
        uses: actions/upload-artifact@v4
        with:
          name: CapillimetryPortableZip
          path: CapillimetryPortable.zip
          if-no-files-found: error